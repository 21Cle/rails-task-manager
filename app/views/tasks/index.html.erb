
<div class="container">
<h1 ><strong>My tasks</strong></h1>

<ul>
  <% @tasks.each do |task| %>
  <li class="alink"><%= link_to task.title, task_path(task) %> - <%= link_to "🗑", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %> </li>
  <% end %>
</ul>

<div id="special"><%= link_to '+ Add a new task', new_task_path %>
</div>
</div>
